engine:
  host: 0.0.0.0
  port: 5000
  # Model variant: "pi0" or "pi0.5"
  model_variant: "pi0.5"
  model: /share/project/fengyupu/hf_hub/models/lerobot/pi05_base
  tokenizer: /share/project/fengyupu/hf_hub/paligemma-3b-pt-224
  stat_path: /share/project/fengyupu/hf_hub/aloha_mobile_cabinet/meta/stats.json
  device: "cuda"
  # By default, Pi0.5 uses quantiles for state and action normalization, if false, it uses mean and std instead
  use_quantiles: false

generate:
  images_keys:
    - observation.images.base_0_rgb
    - observation.images.left_wrist_0_rgb
    - observation.images.right_wrist_0_rgb
  # Only used for warmup
  images_shape: [3, 480, 640]
  state_key: observation.state
